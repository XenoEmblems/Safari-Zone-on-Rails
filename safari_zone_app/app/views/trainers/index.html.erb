<!-- Only people signed in should be here -->

<% link_to "Back to Entrance", root_path %>
<% @trainers.each do |trainer| %>
<div class="pokedex">
    <br>
    <img src="<%= trainer.image_url %>" width="250" height="250"><br>
    <h2><%= trainer.username %></h2>
    <%= link_to "View Trainer Profile", trainer_path(trainer) %><br>
    <br>
</div>
<% end %>

    
